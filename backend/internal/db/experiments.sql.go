// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: experiments.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createExperiment = `-- name: CreateExperiment :one
INSERT INTO experiments (
    mode,
    metrics
) VALUES (
    $1, $2
)
RETURNING id, run_at, mode, metrics, created_at
`

type CreateExperimentParams struct {
	Mode    string `json:"mode"`
	Metrics []byte `json:"metrics"`
}

func (q *Queries) CreateExperiment(ctx context.Context, arg CreateExperimentParams) (Experiment, error) {
	row := q.db.QueryRow(ctx, createExperiment, arg.Mode, arg.Metrics)
	var i Experiment
	err := row.Scan(
		&i.ID,
		&i.RunAt,
		&i.Mode,
		&i.Metrics,
		&i.CreatedAt,
	)
	return i, err
}

const createExperimentResult = `-- name: CreateExperimentResult :one
INSERT INTO experiment_results (
    experiment_id,
    case_id,
    mode,
    kpi_deviation,
    cvar,
    violations,
    reproducibility,
    metrics
) VALUES (
    $1, $2, $3, $4, $5, $6, $7, $8
)
RETURNING id, experiment_id, case_id, mode, kpi_deviation, cvar, violations, reproducibility, metrics, created_at
`

type CreateExperimentResultParams struct {
	ExperimentID    pgtype.UUID    `json:"experiment_id"`
	CaseID          pgtype.UUID    `json:"case_id"`
	Mode            string         `json:"mode"`
	KpiDeviation    pgtype.Numeric `json:"kpi_deviation"`
	Cvar            pgtype.Numeric `json:"cvar"`
	Violations      int32          `json:"violations"`
	Reproducibility pgtype.Numeric `json:"reproducibility"`
	Metrics         []byte         `json:"metrics"`
}

func (q *Queries) CreateExperimentResult(ctx context.Context, arg CreateExperimentResultParams) (ExperimentResult, error) {
	row := q.db.QueryRow(ctx, createExperimentResult,
		arg.ExperimentID,
		arg.CaseID,
		arg.Mode,
		arg.KpiDeviation,
		arg.Cvar,
		arg.Violations,
		arg.Reproducibility,
		arg.Metrics,
	)
	var i ExperimentResult
	err := row.Scan(
		&i.ID,
		&i.ExperimentID,
		&i.CaseID,
		&i.Mode,
		&i.KpiDeviation,
		&i.Cvar,
		&i.Violations,
		&i.Reproducibility,
		&i.Metrics,
		&i.CreatedAt,
	)
	return i, err
}

const getExperiment = `-- name: GetExperiment :one
SELECT id, run_at, mode, metrics, created_at
FROM experiments
WHERE id = $1
`

func (q *Queries) GetExperiment(ctx context.Context, id pgtype.UUID) (Experiment, error) {
	row := q.db.QueryRow(ctx, getExperiment, id)
	var i Experiment
	err := row.Scan(
		&i.ID,
		&i.RunAt,
		&i.Mode,
		&i.Metrics,
		&i.CreatedAt,
	)
	return i, err
}

const listExperimentResults = `-- name: ListExperimentResults :many
SELECT id, experiment_id, case_id, mode, kpi_deviation, cvar, violations, reproducibility, metrics, created_at
FROM experiment_results
WHERE experiment_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListExperimentResults(ctx context.Context, experimentID pgtype.UUID) ([]ExperimentResult, error) {
	rows, err := q.db.Query(ctx, listExperimentResults, experimentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ExperimentResult
	for rows.Next() {
		var i ExperimentResult
		if err := rows.Scan(
			&i.ID,
			&i.ExperimentID,
			&i.CaseID,
			&i.Mode,
			&i.KpiDeviation,
			&i.Cvar,
			&i.Violations,
			&i.Reproducibility,
			&i.Metrics,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listExperiments = `-- name: ListExperiments :many
SELECT id, run_at, mode, metrics, created_at
FROM experiments
ORDER BY run_at DESC, created_at DESC
`

func (q *Queries) ListExperiments(ctx context.Context) ([]Experiment, error) {
	rows, err := q.db.Query(ctx, listExperiments)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Experiment
	for rows.Next() {
		var i Experiment
		if err := rows.Scan(
			&i.ID,
			&i.RunAt,
			&i.Mode,
			&i.Metrics,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateExperimentMetrics = `-- name: UpdateExperimentMetrics :one
UPDATE experiments
SET metrics = $2
WHERE id = $1
RETURNING id, run_at, mode, metrics, created_at
`

type UpdateExperimentMetricsParams struct {
	ID      pgtype.UUID `json:"id"`
	Metrics []byte      `json:"metrics"`
}

func (q *Queries) UpdateExperimentMetrics(ctx context.Context, arg UpdateExperimentMetricsParams) (Experiment, error) {
	row := q.db.QueryRow(ctx, updateExperimentMetrics, arg.ID, arg.Metrics)
	var i Experiment
	err := row.Scan(
		&i.ID,
		&i.RunAt,
		&i.Mode,
		&i.Metrics,
		&i.CreatedAt,
	)
	return i, err
}
